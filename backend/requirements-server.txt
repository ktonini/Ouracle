# Minimal pinned dependencies for the headless read-only API server
# (backend.src.mobile_server). Deliberately excludes desktop-only deps:
# playwright, langchain*, faiss-cpu, pandas.
#
# Versions match what backend/tests passes against.
fastapi==0.141.1
starlette==1.3.1
pydantic==2.13.4
sqlalchemy==2.0.51
uvicorn[standard]==0.34.0
# Oura API v2 ingest adapter (backend.src.oura_v2)
httpx==0.28.1

# Test/dev extras (uncomment locally; not installed in the container image):
# pytest==9.1.1
# httpx==0.28.1
